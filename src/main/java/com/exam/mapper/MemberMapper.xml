<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.exam.mapper.MemberMapper">
 
 
     <insert id="save" parameterType="Member">
        insert into member (member_idx, member_id, member_pw, member_name, member_phone, member_date,member_address, member_manager)
        values (#{member_idx}, #{member_id}, #{member_name}, #{member_phone}, #{member_date}, #{member_address}, ${member_manager} )
     </insert>
   
     <select id="findById" parameterType="string" resultType="Member">
        select member_idx, member_id, member_pw, member_name, member_phone, member_date, member_address, member_manager
        from member
        where member_idx= #{member_idx}
     </select>
</mapper>


